<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simple_forum">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Simple_forum"
        android:windowSoftInputMode="adjustPan">
        <activity
            android:name=".ui.discussion_view.NewDiscussionFormActivity"
            android:exported="false" />
        <activity
            android:name=".ui.discussion_view.DiscussionViewActivity"
            android:exported="false" />
        <activity
            android:name=".ui.discussion_view.DiscussionListActivity"
            android:exported="true" />
        <activity
            android:name=".ui.topic_view.TopicListActivity"
            android:exported="true"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>